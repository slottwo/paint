#include "events.h"
#include "../types/data.h"

enum events CURRENT_EVENT = EVENT_SELECT;

int selectOperation(int x, int y)
{
}

int createOperation(enum operation op, int x, int y)
{
    switch (SELECTED.type)
    {
    case point_type:
        
        break;

    default:
        break;
    }
}

int moveOperation(int x, int y)
{
}

int rotateOperation(int x, int y)
{
}

int scaleOperation(int x, int y)
{
}
